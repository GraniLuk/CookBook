# NOTE: This workflow is now redundant - validation is integrated into hugo.yml
# Kept for reference but disabled. Delete this file if no longer needed.

name: Validate Content (DISABLED)

on:
  workflow_dispatch:  # Manual trigger only

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v6

    - name: Set up Python
      uses: actions/setup-python@v6
      with:
        python-version: '3.x'

    - name: Install Python dependencies
      run: |
        python -m pip install --upgrade pip
        pip install python-frontmatter pyyaml

    - name: Validate Frontmatter
      run: python scripts/normalize_frontmatter.py --check

    - name: Validate Recipe Categories
      shell: pwsh
      run: ./scripts/sync-recipe-categories.ps1 -CheckOnly
